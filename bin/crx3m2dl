#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

product_version='73.0.3683'
acceptformat='crx3'

"${DIR}/crxdl" "$1" "$2" "$3" "$4" "$5" "$6" "$product_version" "$acceptformat"
