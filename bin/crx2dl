#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

product_version='61.0.3163.100'
acceptformat='crx2'

"${DIR}/crxdl" "$1" "$2" "$3" "$4" "$5" "$6" "$product_version" "$acceptformat"
